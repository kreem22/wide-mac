[security]
# Filesystem security mode:
#   strict       -> paths must remain inside USER_DATA_BASE_PATH (raises error on escape)
#   unrestricted -> all paths allowed, both inside and outside workspace
#   elevated     -> paths outside workspace allowed but validated

mode = junk
